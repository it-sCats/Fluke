import 'package:flutter/material.dart';

class notifaction extends StatelessWidget {
  const notifaction({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('N'));
  }
}
