import 'package:flutter/material.dart';

class Oevents extends StatelessWidget {
  const Oevents({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('E'));
  }
}
